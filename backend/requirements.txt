# Audio Processing API – Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# For GPU-accelerated Whisper / pyannote, follow their individual
# installation guides and replace the CPU packages below as needed.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Audio processing ──────────────────────────────────────────────────────────
ffmpeg-python>=0.2.0

# ── Transcription / diarisation ───────────────────────────────────────────────
# Uncomment and pin the version that matches your hardware / CUDA version.
# openai-whisper>=20231117
# pyannote.audio>=3.1.1

# ── LLM providers (summarisation) ─────────────────────────────────────────────
# Installed selectively depending on which providers you use.
# openai>=1.30.0
# anthropic>=0.28.0
# google-generativeai>=0.5.0
# groq>=0.9.0
# langchain-ollama>=0.1.0

# ── Authentication ─────────────────────────────────────────────────────────────
bcrypt>=4.1.2
PyJWT>=2.8.0
# Google OAuth ID token verification (only needed when GOOGLE_CLIENT_ID is set)
google-auth>=2.29.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
python-multipart>=0.0.9   # required by FastAPI for Form / UploadFile
pydantic[email]>=2.7.0
